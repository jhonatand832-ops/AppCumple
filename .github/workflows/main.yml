name: Build APK
on: [push]

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v3
      - name: Build with Buildozer
        uses: Enenkel/buildozer-action@v0.0.2
        with:
          command: buildozer android debug

